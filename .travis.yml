language: python
python:
  - "3.7"

# command to install dependencies
#install:
#  - pip install -r requirements.txt

# command to run tests
script:
  - cd State
  - python -m unittest state_pattern_test.py
